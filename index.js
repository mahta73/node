require('./nodeBook/yek');

/*require('./app/index');
const _ = require('ladash');
const http = require('http');
 Node.js uses asynchronous programming
 node.js file system module allow you to work with the file system on your computer
 const fs = require('fs')
 Read, create, delete, rename, update files
To include a module, use the require() function with the name of the module

const hostname = '127.0.0.1';
const port = 3000;
const server = http.createServer(function(req, res) {

}).listen(port);

_.assign({ 'a': 1 }, { 'b': 2 }, { 'c': 3 })

 configuring http server
 The HTTP module can create an HTTP server that listens to server ports and gives a response back to the clien

The function passed into the http.createServer() method,
will be executed when someone tries to access the computer on port 8080
http.createServer(function(req, res) {
  res.writeHead(200)
  res.write('node.js is really fun')
  res.end()
}).listen(8080)


response.wireHead() sends a response
response.end('Hello, World!') tells the server that all response headers as well as the body
have been sent


//Use the exports keyword to make properties and methods available outside the module file

exports.myDateTime() = function() {
  return new Date()
}
var dt = require('./myfirstmodule')
dt.myDateTime()
*/
